const NUI_EXTRA_DELAY = 10; // Minimal delay for NUI interactions

export default {
    NUI_EXTRA_DELAY,
    // Add other minimal state properties if absolutely necessary for job_selection_menu.js
};
